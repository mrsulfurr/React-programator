import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <header className="jumbotron Header">
            <h1>Auction portal header</h1>
        </header>
    );
};

export default Header;